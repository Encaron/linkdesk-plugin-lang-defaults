{
  "name": "linkdesk-plugin-lang-defaults",
  "version": "1.0.17",
  "private": true,
  "type": "module",
  "description": "出厂中文/English 语言包——可卸载，卸载后 t(key) 返回 key 原文——纯数据插件（无 entry/无编译表面）。独立仓可自足构建：build 走 @linkdesk/plugin-sdk 的 pack 通道（目录整树打包），不产 bundle。",
  "scripts": {
    "build": "linkdesk-plugin-sdk pack",
    "validate": "linkdesk-plugin-sdk validate",
    "lint": "linkdesk-plugin-sdk lint",
    "publish": "linkdesk-plugin-sdk publish"
  },
  "devDependencies": {
    "@linkdesk/plugin-sdk": "^0.1.14"
  }
}
